#
# wm independent hotkeys
#

# terminal emulator
super + Return
	   kitty

# Open editor
super + c
    cblt

# Open process viewer
super + p
    kitty htop

# rofi
super + grave
    	rofi -show drun \
         -p "> " \
         -mesg "[ LAUNCH ]" \
         -theme ~/.config/rofi/peakyArch.rasi

# reload bspwm
super + shift + r
	   bspc wm -r

# make sxhkd reload its configuration files:
super + Escape
	   pkill -USR1 -x sxhkd

#
# bspwm hotkeys
#

# TODO print screen
# super + Print
#   ~/.config/bspwm/misc/screenshot

# TODO fullscreen
# super + f
#    ~/.config/bspwm/misc/full

# Close window
super + q
    bspc node -c

# Open firefox
super + f
    firefox

# Screenshot selection -> save + clipboard
Print
    FILE=~/Pictures/Screenshots/$(date +%Y-%m-%d_%H-%M-%S).png; maim -s "$FILE" && xclip -selection clipboard -t image/png -i "$FILE"

#
# move/resize
#

# switch
super + {_, shift + }{1-9, 0}
	bspc {desktop -f, node -d} '^{1-9, 10}'

# expand a window by moving one of its side outward
super + shift + {Left,Down,Up,Right}
	bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}
# contract a window by moving one of its side inward
super + alt + shift + {Left,Down,Up,Right}
	bspc node -z {right -20 0,top 0 20,bottom 0 -20,left 20 0}

# focus the node in the given direction (arrow keys)
super + {Left,Down,Up,Right}
	bspc node -f {west,south,north,east}

# Audio

# mute
XF86AudioMute
    wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle

# volume down
XF86AudioLowerVolume
    wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-

# volume up
XF86AudioRaiseVolume
    wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+